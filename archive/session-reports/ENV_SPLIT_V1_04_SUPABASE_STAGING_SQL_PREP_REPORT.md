# ENV_SPLIT_V1_04 — SUPABASE STAGING SQL PREP — REPORT

**Data:** 2026-06-17
**Tipo:** SOLO preparazione file locali. Nessun deploy, nessun commit, nessuna
scrittura DB, nessuna creazione progetto Supabase. Schema prod letto in **read-only**
(solo metadati, **zero righe di dati reali**).

---

## FASE 1 — Audit schema prod (read-only)

Sorgente: progetto PROD `wnswassgfuuivmfwjxsf` via MCP `list_tables` (verbose) +
query su `pg_policies` / `pg_indexes` / `information_schema` / `pg_proc` / `pg_catalog`.
Letti **solo nomi di chiave** in `config` (mai i valori; segreti esclusi).

### Inventario (15 tabelle, schema `public`) — senza PII
| Tabella | PK | RLS | Note |
|---|---|---|---|
| `ordenes` | id (text) | ON + policy SELECT | FK → clientes, manual_giros; molte colonne planner/timestamp |
| `wa_msgs` | id (text) | ON + policy SELECT | |
| `conv` | wa_id (text) | ON + policy SELECT | |
| `clientes` | id (bigint seq) | ON + policy SELECT | 212 righe in prod (NON copiate) |
| `storico` | id (int seq) | ON + policy SELECT | FK → clientes; 497 righe (NON copiate) |
| `config` | chiave (text) | ON + policy SELECT (anon, esclude 5 segreti) | 23 righe |
| `archivio_conv` | id (bigint seq) | ON + policy SELECT | |
| `analisi_serata` | data (date) | ON + policy SELECT | |
| `suggerimenti` | id (text) | ON + policy SELECT | |
| `delivery_logs` | id (int seq) | ON, **nessuna policy** (solo service_role) | |
| `geo_cache` | direccion_key (text) | ON + policy ALL (public r/w) | 369 righe (NON copiate) |
| `backup_serata` | id (int seq) | ON, **nessuna policy** | |
| `serata_summary` | fecha (date) | ON, **nessuna policy** | |
| `manual_giros` | id (text) | **OFF** ⚠️ | FK target per ordenes.manual_giro_id |
| `orden_estado_logs` | id (uuid) | **OFF** ⚠️ | audit append-only non-PII |

- **Indici:** 40 (PK + univoci parziali + indici di query) — replicati 1:1.
- **FK:** `ordenes.cliente_id→clientes.id`, `ordenes.manual_giro_id→manual_giros.id`,
  `storico.cliente_id→clientes.id` — replicate (ordine di creazione rispettato).
- **Constraint UNIQUE/CHECK:** unici notevoli replicati
  (`storico (orden_id,fecha)`, `clientes_tel_uniq`/`alias_uniq` parziali,
  `ordenes_client_req_id_uniq` parziale, `manual_giros (giro_day,seq)`,
  `archivio_conv (wa_id,data_servizio)`). I CHECK sono solo NOT NULL impliciti.
- **Functions/Triggers custom:** **NESSUNO** nello schema public (verificato).
- **Enum/Type custom:** nessuno (gli "stati" sono `text` liberi, non enum).
- **⚠️ Advisory sicurezza (critical):** `manual_giros` e `orden_estado_logs`
  hanno **RLS disabilitato** in prod → esposte ad anon. Replicato in staging per
  parità, **segnalato** all'utente, SQL di hardening fornito ma NON applicato.

---

## FASE 2–5 — File preparati (NON eseguiti)

| File | Cosa contiene |
|---|---|
| `supabase_staging_schema_v1.sql` | 15 `CREATE TABLE IF NOT EXISTS` + 40 indici + FK + RLS enable + 10 policy. Seriali resi con `GENERATED … AS IDENTITY`. Avviso RLS-OFF + lista "non esportabile" (Storage/Auth/Edge). |
| `supabase_staging_seed_v1.sql` | `config` con **valori staging placeholder** (PIN `123456`/`654321` ≠ prod, `AUTO_RISPOSTA/WEBHOOK_ACTIVE=FALSE`, `GEO_PROVIDER=haversine`); 2 `clientes` fake + 2 `geo_cache` fake con marker `STAGING_SEED_`. **Nessun ordine, nessun PII.** |
| `supabase_staging_test_cleanup.sql` | DELETE dei soli `TEST_V1_STAGING_%` su 6 tabelle (ordine FK-safe), con **guard anti-prod** (aborta se `PIZZERIA_NOME` non contiene `STAGING`) + conteggi BEFORE/AFTER. Non tocca il seed. |
| `ENV_SPLIT_V1_SUPABASE_STAGING_README.md` | Procedura: creare progetto → schema → seed → recupero chiavi → dove incollarle (Railway V1 / Netlify functions / CRA build) → verifica isolamento → cosa NON fare. |

### Scelte di fedeltà / note
- Colonne `id` seriali prod (`nextval(seq)`) → `GENERATED BY DEFAULT AS IDENTITY`
  (equivalente funzionale, staging riparte da 1). Documentato nello schema.
- RLS replica **esattamente** lo stato prod, incluse le 2 tabelle con RLS OFF
  (necessario perché backend usa service_role e frontend anon vi accede senza policy).
- Valori config a formato incerto (`DRIVER_STATO`, `REPARTIDORES`, `PIANO`,
  `SCADENZA`, `AI_MODELLO`) marcati con `⚠️ ADATTARE` nel seed.
- Marker: `STAGING_SEED_` (dati baseline) vs `TEST_V1_STAGING_%` (dati di test runtime, cleanabili).

---

## VALIDAZIONE

**Grep anti-PII / segreti** (sui 4 file):
- ✅ Nessun segreto letterale (`service_role`, JWT `eyJ…`, `sk-ant`, `sb_secret`, `sb_publishable_…`, `ld_…`).
- ✅ Nessuna email.
- ✅ Telefoni presenti = solo fake `+340000000001/02` (marker `STAGING_SEED_`).
- ✅ Ref prod `wnswassgfuuivmfwjxsf` compare **solo** in 1 commento dello schema (provenienza) e nel README come "NON usare".

**Sanity SQL** (psql non installato → controllo euristico):
- ✅ Parentesi bilanciate in tutti i file (96/96, 43/43, 34/34).
- ✅ `begin;`/`commit;` bilanciati (1/1 per file).
- ✅ `CREATE TABLE` = 15 (atteso 15); `CREATE POLICY` = 10 (atteso 10).
- ⚠️ Parse reale su Postgres non eseguibile localmente (no psql/connessione). La
  validazione definitiva avverrà eseguendo lo schema sul progetto staging vuoto.

---

## FILE CREATI (in `/Users/bigart/Downloads/LaDieciBotV2-github/`)
- `supabase_staging_schema_v1.sql`
- `supabase_staging_seed_v1.sql`
- `supabase_staging_test_cleanup.sql`
- `ENV_SPLIT_V1_SUPABASE_STAGING_README.md`
- `ENV_SPLIT_V1_04_SUPABASE_STAGING_SQL_PREP_REPORT.md` (questo)

## PROSSIMO STEP
Utente: crea il progetto Supabase staging, poi esegue `schema` → `seed` (FASE 2–3
del README). Quando il progetto esiste e hai le chiavi, posso (se autorizzi)
impostare le env sul sito Netlify V1 `a3ad035a` e procedere alla verifica isolamento.

**STOP.** Nessun SQL eseguito, nessuna scrittura Supabase, nessun commit/deploy.
`ORDINI_2026-05-23.md` e production non toccati.
